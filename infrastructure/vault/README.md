# Vault

Private URL:

```text
https://vault.laperm-dragon.ts.net
```

This is a single-node standalone Vault using Longhorn-backed file storage. Initialize and unseal Vault manually after first deploy. Configure Keycloak OIDC auth inside Vault after Keycloak is available.
